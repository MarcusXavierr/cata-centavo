import type { Rule } from "eslint";

export declare const localRules: {
  readonly rules: {
    readonly "complexity-ceiling": Rule.RuleModule;
    readonly "require-disable-reason": Rule.RuleModule;
  };
};
